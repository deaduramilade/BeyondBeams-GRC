"use client";

import { useMemo, useState } from "react";
import { ExternalLink, Search } from "lucide-react";
import { Input } from "@/components/ui/input";

type Reference = { id: string; framework: string; jurisdiction: string; reference: string; title: string; excerpt: string; metric: string | null; sourceUrl: string; industry: string };
export function ComplianceBrowser({ references, compact = false }: { references: Reference[]; compact?: boolean }) { const [query,setQuery]=useState(""); const matches=useMemo(()=>references.filter((item)=>`${item.framework} ${item.jurisdiction} ${item.reference} ${item.title} ${item.industry}`.toLowerCase().includes(query.toLowerCase())),[references,query]); return <section className="rounded-lg border bg-card"><div className="border-b p-5"><div className="flex items-center justify-between gap-3"><div><h2 className="text-sm font-bold">Worldwide compliance reference</h2><p className="mt-1 text-xs text-muted-foreground">Curated excerpts for assessment context. Verify the current official source before relying on a requirement.</p></div><span className="shrink-0 rounded-full bg-primary/10 px-2 py-1 text-[10px] font-bold text-primary">{references.length} SOURCES</span></div><label className="relative mt-4 block"><Search className="absolute left-3 top-3 size-4 text-muted-foreground"/><Input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="Search ISO, NIST, GDPR, HIPAA, PCI DSS, DORA…" className="pl-9"/></label></div><div className={`${compact ? "max-h-[420px]" : "max-h-[620px]"} divide-y overflow-y-auto`}>{matches.map((item)=><article key={item.id} className="p-5"><div className="flex flex-wrap items-start justify-between gap-2"><div><p className="text-[10px] font-bold uppercase text-primary">{item.framework} · {item.reference}</p><h3 className="mt-1 text-sm font-bold">{item.title}</h3></div><span className="text-[10px] text-muted-foreground">{item.jurisdiction}</span></div><blockquote className="mt-4 border-l-2 border-primary/50 pl-4 text-xs leading-6 text-muted-foreground">“{item.excerpt}”</blockquote>{item.metric&&<div className="mt-3 rounded-md bg-muted p-3"><p className="text-[10px] font-bold uppercase text-muted-foreground">Assessment metric</p><p className="mt-1 text-xs">{item.metric}</p></div>}<a href={item.sourceUrl} target="_blank" rel="noreferrer" className="mt-3 inline-flex items-center gap-1 text-[11px] font-semibold text-primary">Official source <ExternalLink className="size-3"/></a></article>)}</div>{!matches.length&&<p className="p-8 text-center text-sm text-muted-foreground">No matching references.</p>}</section>; }