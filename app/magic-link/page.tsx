import{AuthLayout}from"@/components/auth-layout";import{MagicLinkExchange,MagicLinkForm}from"@/components/auth-forms";
export default async function MagicLinkPage({searchParams}:{searchParams:Promise<{email?:string;token?:string}>}){const{email,token}=await searchParams;return <AuthLayout eyebrow="Passwordless access" title={token?"Secure sign in":"Email me a sign-in link"} description={token?"We are validating this single-use link.":"Links expire after 15 minutes and can only be used once."}>{email&&token?<MagicLinkExchange email={email} token={token}/>:<MagicLinkForm/>}</AuthLayout>}