import { BookOpen,CheckCircle2 } from "lucide-react";
import { PageHeader } from "@/components/page-header";
import { industryTemplates } from "@/lib/industry-library";
export default function LibraryPage(){return <><PageHeader eyebrow="Tailored coverage" title="Healthcare & fintech risk library" description="Ready-to-adapt risk statements, controls, and compliance starting points for regulated operating models."/><div className="grid gap-4 lg:grid-cols-2">{industryTemplates.map(item=><article key={`${item.area}-${item.title}`} className="rounded-lg border bg-card p-5"><div className="flex items-start justify-between"><div><p className="text-[10px] font-bold uppercase text-primary">{item.industry} · {item.area}</p><h2 className="mt-2 text-base font-bold">{item.title}</h2></div><BookOpen className="size-5 text-muted-foreground"/></div><p className="mt-4 text-sm leading-6 text-muted-foreground">{item.risk}</p><div className="mt-5 grid gap-4 border-t pt-4 sm:grid-cols-2"><div><p className="mb-2 text-[10px] font-bold uppercase text-muted-foreground">Suggested controls</p>{item.controls.map(x=><p key={x} className="mt-2 flex gap-2 text-xs"><CheckCircle2 className="size-3.5 shrink-0 text-primary"/>{x}</p>)}</div><div><p className="mb-2 text-[10px] font-bold uppercase text-muted-foreground">Mapping prompts</p>{item.frameworks.map(x=><span key={x} className="mb-1 mr-1 inline-block rounded bg-muted px-2 py-1 text-[10px]">{x}</span>)}</div></div></article>)}</div></>}