import{PageHeader}from"@/components/page-header";import{Card,CardContent}from"@/components/ui/card";export default function Settings(){return <><PageHeader eyebrow="Workspace administration" title="Settings" description="Security, plan, data-retention, and integration settings for the current tenant."/><Card><CardContent className="grid gap-5 pt-5 sm:grid-cols-3">{[["Authentication","Credentials enabled for local demo"],["Data boundary","All records scoped to session tenant"],["Plan","Free workspace · 3 translator uses/user"]].map(([a,b])=><div key={a}><p className="text-xs font-bold">{a}</p><p className="mt-2 text-xs text-muted-foreground">{b}</p></div>)}</CardContent></Card></>}