import{PageHeader}from"@/components/page-header";import{Card,CardContent,CardHeader}from"@/components/ui/card";import{db}from"@/lib/db";import{requireSession}from"@/lib/authz";import{NotificationSettings}from"@/components/notification-settings";import{EmailReportForm}from"@/components/email-report-form";import{Role}from"@prisma/client";export default async function Settings(){const session=await requireSession();const[user,tenant]=await Promise.all([db.user.findUniqueOrThrow({where:{id:session.user.id},select:{email:true,reviewEmailsEnabled:true,assignmentEmailsEnabled:true,exportEmailsEnabled:true}}),db.tenant.findUniqueOrThrow({where:{id:session.user.tenantId},select:{reviewRemindersEnabled:true,reviewReminderCadence:true,plan:true}})]);return <><PageHeader eyebrow="Workspace administration" title="Settings" description="Manage secure workspace controls and the email notifications that keep risk work moving."/><div className="grid gap-5"><Card><CardContent className="grid gap-5 pt-5 sm:grid-cols-3">{[["Authentication","Credentials enabled for local demo"],["Data boundary","All records scoped to session tenant"],["Plan",`${tenant.plan} workspace`]].map(([a,b])=><div key={a}><p className="text-xs font-bold">{a}</p><p className="mt-2 text-xs text-muted-foreground">{b}</p></div>)}</CardContent></Card><NotificationSettings user={user} tenant={tenant} owner={session.user.role===Role.OWNER}/><Card><CardHeader><div><h2 className="text-sm font-bold">Email a report</h2><p className="mt-1 text-xs text-muted-foreground">Generate a quota-controlled export and send a secure download link that expires after 24 hours.</p></div></CardHeader><CardContent><EmailReportForm email={user.email}/></CardContent></Card></div></>}