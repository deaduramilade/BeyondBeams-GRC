import { redirect } from "next/navigation";
import { auth } from "@/auth";
import { LoginForm } from "@/components/login-form";
import { Brand } from "@/components/brand";
export default async function LoginPage() { if (await auth()) redirect("/app"); return <main className="grid min-h-screen bg-[#071c2f] lg:grid-cols-[1.1fr_.9fr]"><section className="relative hidden overflow-hidden border-r border-white/10 p-12 lg:flex lg:flex-col lg:justify-between"><Brand/><div className="relative z-10 max-w-xl"><p className="mb-5 text-xs font-bold uppercase tracking-[.18em] text-primary">Risk intelligence, clearly aligned</p><h1 className="font-display text-6xl leading-[1.02] text-white">See the risks.<br/><span className="italic text-sky-200">Strengthen the business.</span></h1><p className="mt-7 max-w-md text-base leading-7 text-slate-400">One accountable workspace for assessing, treating, and reviewing the risks that shape your organisation.</p></div><div className="grid grid-cols-5 gap-2 opacity-70" aria-hidden>{Array.from({ length: 20 }, (_, i) => <span key={i} className={`h-2 rounded-sm ${[4,8,9,13].includes(i) ? "bg-primary" : i % 3 === 0 ? "bg-sky-300/50" : "bg-white/10"}`}/>)}</div></section><section className="flex min-h-screen items-center justify-center p-6"><div className="w-full max-w-sm"><Brand className="mb-12 lg:hidden"/><div className="mb-8"><p className="mb-2 text-xs font-bold uppercase tracking-[.16em] text-primary">Secure workspace</p><h2 className="font-display text-4xl text-white">Welcome back</h2><p className="mt-2 text-sm text-slate-400">Sign in to your risk workspace.</p></div><LoginForm/></div></section></main>; }